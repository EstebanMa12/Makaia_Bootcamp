const formulario = document.getElementById('producto')
formulario.addEventListener('submit',(event)=>{
    event.preventDefault();
    const newformdata = new FormData(formulario);
    
    const jsonData ={};
    for (let[key])
})